# Python
__pycache__/
*.py[cod]
*.pyc
*.pyc.*
*.egg-info/
.venv/
venv/

# Claude Code local config
.claude/

# OS / editor
.DS_Store
Thumbs.db

# Large raw data and binaries — not for version control (too big / not source)
data/raw/ml/*.exe
data/raw/ml/*.winmd
data/raw/labels/
data/raw/landsat/
*.tif
*.tiff
*.exe

# Streamlit secrets (keep .streamlit/config.toml)
.streamlit/secrets.toml
